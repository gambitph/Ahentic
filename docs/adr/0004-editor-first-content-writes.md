# Editor-open content writes use browser abilities

When page context shows the block editor open for a post, content edits for that post go through browser/editor abilities so the canvas stays source of truth. Outside the editor, server content abilities are used. Dual-equal model choice was rejected because it causes desync and half-finished tasks.
